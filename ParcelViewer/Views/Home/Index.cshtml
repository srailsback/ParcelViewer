@{
    ViewBag.Title = "Index";
}

<h2>Listing Parcels</h2>
<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Parcel Number</th>
            <th>Address</th>
            <th>Owner</th>
        </tr>
    </thead>
    @*<tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ParcelNumber</td>
                <td>@item.Address</td>
                <td>@item.Owner</td>
            </tr>
        }
    </tbody>*@

</table>

@section styles{ 
    @Styles.Render("~/content/css/datatables")
}


@section scripts {
    @Scripts.Render("~/bundles/datatables")
    <script>
        $(document).ready(function () {
            $('table').dataTable({
                "processing": true,
                "serverSide": true,
                "ajax": '@Url.Action("GetData", "Home")'
            });

        });
    </script>
}